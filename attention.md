📋 ВЫЖИМКА ЗНАНИЙ О ПРОЕКТЕ
Структура проекта
/Users/agdelesha/Desktop/myScripts/wgScript/
├── vpn_bot/          # Основной VPN бот
│   ├── handlers/     # Обработчики (user.py, admin.py)
│   ├── services/     # Сервисы (wireguard_multi.py, ai_assistant.py)
│   ├── keyboards/    # Клавиатуры (user_kb.py, admin_kb.py)
│   └── database/     # Модели БД
└── deploy_bot/       # Бот для деплоя и управления серверами


Турция	83.217.9.75	Pidrpidr2007Pidr	Основной VPN бот
Финляндия	185.42.163.129	xA6mW5tR9lpE	VPN сервер с V2Ray
россия 45.143.203.153 CPbOFaueO8446 сервер для деплойбота


# 1. Копируем файлы на сервер
sshpass -p 'Pidrpidr2007Pidr' scp файл root@83.217.9.75:/root/vpn_bot/путь/

# 2. Перезапускаем сервис
sshpass -p 'Pidrpidr2007Pidr' ssh root@83.217.9.75 "systemctl restart vpn-bot"

# 3. Пушим в git
git add -A && git commit -m "описание" && git push origin main


Git
Репозиторий: https://github.com/agdelesha/agdeweb
Ветка: main
DeepSeek AI
Используется для:
Ответы на вопросы пользователей в чате
Генерация инструкций при создании конфига (generate_welcome_instruction)
Генерация приветствия при активации пробного периода (generate_trial_activated_message)
НЕ используется для главного меню (слишком медленно)
Важные файлы
vpn_bot/handlers/user.py — основная логика пользователя, воронка
vpn_bot/handlers/admin.py — админ-панель
vpn_bot/services/wireguard_multi.py — работа с серверами, создание/удаление конфигов
vpn_bot/services/ai_assistant.py — интеграция с DeepSeek
deploy_bot/bot.py — управление серверами, установка AWG/V2Ray
Особенности удаления конфигов
Метод delete_config автоматически определяет тип по имени:
v2ray_* → вызывает delete_v2ray_config
awg_* → вызывает delete_awg_config
остальные → WireGuard удаление
Проверка доступности протоколов
check_awg_available(server) — проверяет наличие awg-quick
check_v2ray_available(server) — проверяет наличие скрипта и активность xray